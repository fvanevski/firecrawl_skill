"""Claim-to-evidence semantic binding boundary.

The implementation stays at its baseline-tracked path for #264; #269 owns the
final compatibility cleanup after existing type debt is resolved explicitly.
"""

from ..claim_binding_service import ClaimBindingService

__all__ = ["ClaimBindingService"]
